10 PRINT "Enter text to encrypt: "
20 INPUT INPUT_TEXT$
30 PRINT "Enter shift value: "
40 INPUT SHIFT
50 GOSUB 200
60 PRINT "Encrypted text: "; OUTPUT_TEXT$

70 PRINT "Enter text to decrypt: "
80 INPUT INPUT_TEXT$
90 PRINT "Enter shift value: "
100 INPUT SHIFT
110 GOSUB 300
120 PRINT "Decrypted text: "; OUTPUT_TEXT$

130 PRINT "Enter text for brute-force solve: "
140 INPUT INPUT_TEXT$
150 GOSUB 400
160 END

200 REM ENCRYPTION SUBROUTINE
210 OUTPUT_TEXT$ = ""
220 FOR I = 1 TO LEN(INPUT_TEXT$)
230     ASCII = ASC(MID$(INPUT_TEXT$, I, 1))
240     IF ASCII >= 97 AND ASCII <= 122 THEN ASCII = ASCII - 32
250     IF ASCII >= 65 AND ASCII <= 90 THEN ASCII = ((ASCII - 65 + SHIFT) MOD 26) + 65
260     OUTPUT_TEXT$ = OUTPUT_TEXT$ + CHR$(ASCII)
270 NEXT I
280 RETURN

300 REM DECRYPTION SUBROUTINE
310 OUTPUT_TEXT$ = ""
320 FOR I = 1 TO LEN(INPUT_TEXT$)
330     ASCII = ASC(MID$(INPUT_TEXT$, I, 1))
340     IF ASCII >= 97 AND ASCII <= 122 THEN ASCII = ASCII - 32
350     IF ASCII >= 65 AND ASCII <= 90 THEN ASCII = ((ASCII - 65 - SHIFT + 26) MOD 26) + 65
360     OUTPUT_TEXT$ = OUTPUT_TEXT$ + CHR$(ASCII)
370 NEXT I
380 RETURN

400 REM BRUTE-FORCE SOLVING SUBROUTINE
410 PRINT "Solving the cipher with all 26 possible shifts:"
420 FOR S = 1 TO 26
430     OUTPUT_TEXT$ = ""
440     FOR I = 1 TO LEN(INPUT_TEXT$)
450         ASCII = ASC(MID$(INPUT_TEXT$, I, 1))
460         IF ASCII >= 97 AND ASCII <= 122 THEN ASCII = ASCII - 32
470         IF ASCII >= 65 AND ASCII <= 90 THEN ASCII = ((ASCII - 65 - S + 26) MOD 26) + 65
480         OUTPUT_TEXT$ = OUTPUT_TEXT$ + CHR$(ASCII)
490     NEXT I
500     PRINT "Shift "; S; ": "; OUTPUT_TEXT$
510 NEXT S
520 RETURN